# Calcium imaging

Align calcium-imaging time series and extract normalised intensity traces
(Fiji only).

> **Stub — rewrite for v2.** Requires the **Template Matching** plugin (unlisted
> update site — see [Update sites](../getting-started/update-sites.md)). UI
> reference: `UI/panes/Tools/CalciumImagingPane`,
> `WorkflowDashboards/AlignStackDashboard`,
> `WorkflowDashboards/CalciumImagingAnalysisDashboard`.

**Outline**
- Adding the Template Matching site
- Aligning slices in a stack
- Selecting cells / ROIs
- Extracting and normalising traces
- Outputs

> Preflight warns (non-blocking) if the "Align slices in stack…" command is
> missing.
