# Ganglia & cells per ganglion

Segment ganglia and normalise neuron / subtype counts to the number of ganglia.

> **Stub — rewrite for v2.** UI reference: ganglia handling in the neuron
> workflow and `UI/panes/Tools/dialogs/GangliaExpansionDialog`.

**Outline**
- Inputs (Hu + a neuronal/glial marker)
- Running the DeepImageJ ganglia model
- Ganglia expansion / editing
- Cells per ganglion and normalised counts

_Screenshots available:_ `wiki_images/Cell_per_ganglia/` (e.g.
`30_Cell_per_ganglia_.png`, `34_…`, `35_…`, and the `hu_only/` subfolder).
