# Multiple markers / multichannel

Analyse images with several markers at once, including multiplexed / registered
channels.

> **Stub — rewrite for v2.** UI reference: `UI/panes/Tools/MultiplexPane` and
> the `services/multiplex/` registration service.

**Outline**
- Loading a multichannel / multiplex image
- Channel assignment
- Optional registration across rounds (`MultiplexRegistrationService`)
- Combining marker results

_Screenshots available:_ `wiki_images/multiple_markers/`.
